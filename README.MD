# Blazium CLI Tool

Command line tool for downloading engine and templates.

## Usage

```sh
chmod +x blazium-cli
./blazium-cli --get-version latest --editor --platform windows --arch x86_64 ./out
```
