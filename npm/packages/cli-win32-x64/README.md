# @blazium-engine/cli-win32-x64

Windows x64 binary for [@blazium-engine/cli](https://www.npmjs.com/package/@blazium-engine/cli).

Install the wrapper, not this package:

```bash
npm install -g @blazium-engine/cli
```
