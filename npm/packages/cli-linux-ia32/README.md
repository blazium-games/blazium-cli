# @blazium-engine/cli-linux-ia32

Linux x86 binary for [@blazium-engine/cli](https://www.npmjs.com/package/@blazium-engine/cli).

Install the wrapper, not this package:

```bash
npm install -g @blazium-engine/cli
```
