# @blazium-engine/cli-linux-x64

Linux x64 binary for [@blazium-engine/cli](https://www.npmjs.com/package/@blazium-engine/cli).

Install the wrapper, not this package:

```bash
npm install -g @blazium-engine/cli
```
